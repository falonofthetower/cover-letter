<main>
  <div class="column">
      <header>
        <h1>Edit Project</h1>
      </header>
      <%= render "form", locals: @project %>
      <%= link_to "Delete Project", { action: :destroy, id: @project.id }, method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-default" %>
  </div>

</main>

